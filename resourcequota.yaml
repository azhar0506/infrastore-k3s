apiVersion: v1
kind: ResourceQuota
metadata:
  name: infrastore-quota
  namespace: infrastore
spec:
  hard:
    pods: "10"
    requests.cpu: "1"
    requests.memory: "512Mi"
    limits.cpu: "2"
    limits.memory: "1Gi"
    persistentvolumeclaims: "5"

